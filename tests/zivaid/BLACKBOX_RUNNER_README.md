# Black-Box Runner

Run from repository root:

`python -m unittest tests/zivaid/test_r00_evidence_engine_blackbox.py -v`

The suite uses only synthetic `BBX-*` identifiers and calls the public `validate()` interface. It does not write to the Evidence Register.
